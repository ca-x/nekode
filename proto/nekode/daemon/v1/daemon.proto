syntax = "proto3";

package nekode.daemon.v1;

option go_package = "github.com/ca-x/nekode/gen/go/nekode/daemon/v1;daemonv1";

// Historical entrypoint kept for documentation links. The protocol is split
// by domain across the sibling files in this directory; service.proto owns the
// DaemonControlService RPC surface.
